# xyflow_flutter examples

Gallery app for the Flutter xyflow port.

```sh
flutter run -d chrome
```

On web, open a specific sample with `?example=`:

`basic` · `drag` · `custom` · `edges` · `story` · `connector` · `advanced` · `robot` · `comfyui`
